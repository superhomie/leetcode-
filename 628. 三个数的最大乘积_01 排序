class Solution {
    public int maximumProduct(int[] nums) {
    //要么是三个最大正数/负数的乘积，要么是两个最小的负数和一个最大正数的乘积

    //运用Array和Math类
    //时间复杂度o（nlogn），空间复杂度0（n）
     Arrays.sort(nums);
        int n = nums.length;
        return Math.max(nums[0] * nums[1] * nums[n - 1], nums[n - 3] * nums[n - 2] * nums[n - 1]);

    }
}
